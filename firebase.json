{
  "hosting": {
    "public": "frontend/out",
    "ignore": [
      "firebase.json",
      "**/.*",
      "**/node_modules/**"
    ],
    "rewrites": [
      {
        "source": "**",
        "destination": "/index.html"
      }
    ]
  },
  "_comment": "Note: Next.js with dynamic routes requires either static export (output: 'export') or Firebase Functions for SSR. Current setup uses server-rendered dynamic routes."
}
