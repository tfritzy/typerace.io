{
  "name": "typerace.io",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "local": "spacetime publish --module-path spacetimedb --server local && spacetime generate --lang typescript --out-dir src/module_bindings --module-path spacetimedb",
    "deploy": "spacetime publish --module-path spacetimedb && spacetime generate --lang typescript --out-dir src/module_bindings --module-path spacetimedb"
  },
  "dependencies": {
    "@clockworklabs/spacetimedb-sdk": "^1.3.3",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^7.9.5",
    "spacetime": "^7.11.0",
    "spacetimedb": "^1.7.0"
  },
  "devDependencies": {
    "@types/react": "^18.3.18",
    "@types/react-dom": "^18.3.5",
    "@vitejs/plugin-react": "^5.0.2",
    "typescript": "~5.6.2",
    "vite": "^7.1.5"
  }
}
